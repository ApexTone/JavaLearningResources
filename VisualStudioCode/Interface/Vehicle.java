//Interface contains only method signature and every method count as Public
//EVERY class that use this interface must implement ALL methods
//Interface is similar to abstract class
public interface Vehicle {
    int cashRate();//Only public method signature
    int ePassRate();
    
}